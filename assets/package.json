{
  "name": "ex_teal_pages",
  "private": true,
  "description": "ExTeal Plugin for Application Configuration and Basic CMS",
  "scripts": {
    "build": "node ./build.mjs",
    "watch": "node ./build.mjs --watch",
    "deploy": "node ./build.mjs --deploy"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "esbuild": "^0.19.4",
    "esbuild-vue": "^1.2.2"
  },
  "dependencies": {
    "vue": "^2.6.10",
    "vue-template-compiler": "2.7.14",
    "ex-teal-js": "^1.0.4"
  }
}
